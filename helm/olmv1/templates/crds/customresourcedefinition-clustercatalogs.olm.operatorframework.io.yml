{{- if .Values.options.catalogd.enabled }}
{{- if (eq .Values.options.featureSet "standard") }}
{{ regexReplaceAll "olm.operatorframework.io([^/])" (tpl (.Files.Get "base/catalogd/crd/standard/olm.operatorframework.io_clustercatalogs.yaml") .) (printf "%s${1}" .Values.apiGroup) }}
{{- else if (eq .Values.options.featureSet "experimental") }}
{{ regexReplaceAll "olm.operatorframework.io([^/])" (tpl (.Files.Get "base/catalogd/crd/experimental/olm.operatorframework.io_clustercatalogs.yaml") .) (printf "%s${1}" .Values.apiGroup) }}
{{- else }}
{{- fail "options.featureSet must be set to one of: {standard,experimental}" }}
{{- end }}
{{- end }}
